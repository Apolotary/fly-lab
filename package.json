{
  "name": "ableton-fly",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "A simulated fly garden performs an ambient Ableton piece through movement and fruit visits.",
  "license": "MIT",
  "engines": {
    "node": ">=24.16.0"
  },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "start": "node scripts/start.mjs",
    "demo": "node src/demo.js",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check:privacy": "node scripts/privacy-check.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  },
  "dependencies": {
    "three": "0.186.0"
  }
}
